name: 'ecommerce_analytics'
version: '1.0.0'

profile: 'ecommerce_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  ecommerce_analytics:

    staging:
      +materialized: view
      +tags: ["bronze", "data_engineering"]

    intermediate:
      +materialized: view
      +tags: ["silver", "data_engineering"]

    marts:
      +materialized: table
      +tags: ["gold", "analytics"]